<h1>正在招聘的职位</h1>
<hr>

  <% @cpanel_positions.each do |position| %>
        <%#= image_tag position.avatar.url(:thumb) %>
    <ul>
      <li>
        <h3>
        职位：<%= position.title %> &nbsp;&nbsp;&nbsp;&nbsp;
        <%= link_to '编辑', edit_cpanel_position_path(position),class:"brivates-button" %>
        <%= link_to '删除', cpanel_position_path(position), method: :delete, data: { confirm: '确定要删除这个职位吗？' }, class:"brivates-button" %>
      </h3>
      </li>
    </ul>
  <% end %>
<hr>
  <div style="height:35px;line-height:35px;position:relative">
    <%= link_to '添加新职位', new_cpanel_position_path,class:"brivates-button" %>
  </div>

